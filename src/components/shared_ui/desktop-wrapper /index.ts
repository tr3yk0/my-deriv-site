/* ========================================================================
   Desktop Wrapper — Shared UI Export Layer
   Centralized exports for the desktop trading workspace module.
======================================================================== */

import "./desktop-wrapper.scss";

export { default } from "./desktop-wrapper";
export { default as DesktopWrapper } from "./desktop-wrapper";

export type {
  DesktopWrapperProps,
  DesktopWrapperTheme,
} from "./desktop-wrapper";
